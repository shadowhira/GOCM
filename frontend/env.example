# Frontend environment example
#
# Usage:
# - Copy this file to `frontend/.env.local` (recommended for local dev)
# - Values prefixed with NEXT_PUBLIC_ are exposed to the browser.
#

# Base URL for the ASP.NET Core backend (see frontend/src/lib/axios.ts)
# Example: http://localhost:5171
NEXT_PUBLIC_API_BASE=

# Base URL for the Quiz service API (see frontend/src/api/quizApi.ts)
# Example: https://your-quiz-service.example.com
NEXT_PUBLIC_QUIZ_API_BASE=
